name: Update environment
description: Action to update the version of the environment
inputs:
  ciToken:
    description: CI Github token
    required: true
  environment:
    description: Name of the environment
    required: true
  serviceName:
    description: Name of the service
    required: true
  serviceVersion:
    description: Version of service
    required: true
runs:
  using: node12
  main: index.js
